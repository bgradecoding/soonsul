import HeaderComponent from "@/components/header";

const DetailPage: React.FC = () => {
  return (
    <div>
      <HeaderComponent title="상세 페이지" share={true} />
    </div>
  );
};

export default DetailPage;
