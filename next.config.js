/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["thumb.mt.co.kr", "img.lovepik.com", "cdn.pixabay.com"],
  },
};

module.exports = nextConfig;
